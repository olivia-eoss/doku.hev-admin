---
export interface ProcessItem {
  number: string;
  title: string;
  description: string;
  href?: string;
}

export interface Props {
  title?: string;
  description?: string;
  items: ProcessItem[];
}

const { title, description, items = [] } = Astro.props;
---

<section class="container-content process-section">
  <div class="process-container">
    <div class="process-header">
      <h2>{title}</h2>
      <p>{description}</p>
    </div>

    <div class="process-grid">
      {
        items.map((item) => (
          <a href={item.href ?? "#"} class="process-card card-hover">
            <span class="process-number">{item.number}</span>
            <h3 class="process-title">{item.title}</h3>
            <p class="process-text">{item.description}</p>
          </a>
        ))
      }
    </div>
  </div>
</section>

<style>
  .process-section {
    width: 100%;
    background: var(--white);
  }

  .process-container {
    padding-top: var(--spacing-xl);
    padding-bottom: var(--spacing-xl);
  }

  .process-header {
    text-align: center;
    margin-bottom: var(--spacing-xl);
  }

  .process-header h2 {
    margin: 0 0 var(--spacing-xxs) 0;
  }

  .process-header p {
    margin: 0 auto;
    line-height: 1.5;
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-xs);
  }

  .process-card {
    display: flex;
    flex-direction: column;
    padding: var(--spacing-sm);
    background: var(--green);
    border-radius: var(--spacing-xs);
  }

  .process-number {
    display: block;
    font-size: var(--font-size-p);
    color: var(--white);
    margin-bottom: var(--spacing-xs);
  }

  .process-title {
    margin: 0 0 var(--spacing-xxs) 0;
    font-size: var(--font-size-h3);
    color: var(--white);
  }

  .process-text {
    margin: 0;
    font-size: var(--font-size-p);
    color: var(--white);
    line-height: 1.5;
  }

  @media (max-width: 900px) {
    .process-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 560px) {
    .process-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
